BoatSpotMedia v41.3 Cart Success Logo Fix
Base: v41.2 Cart Success Privacy Discount Fix.
Fixes:
- Payment success for cart checkout is guarded so buyer does not see error after successful payment.
- Same-boat discount message is disabled until creator approval workflow is fully connected.
- Cart checkout metadata sends pending_discount_review=False.
- Cart page hides real filename before payment.
- Floating cart button appears globally so buyer can return to cart.
- Adds uploaded BoatSpotMedia logo as /static/img/logo-header.png and uses it on standalone pages / obvious brand links.
Not touched:
- upload
- R2 delete
- thumbnails generation
- pricing setup

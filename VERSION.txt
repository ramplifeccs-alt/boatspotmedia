BoatSpotMedia v39.5 R2 No Recursion Fix
- Fixes maximum recursion depth exceeded in R2 client.
- Ensures _client() creates boto3 client directly.
- Ensures get_r2_client() only wraps _client(), never loops.
- Rebuilds multipart helpers cleanly.
- Keeps multipart + fallback uploader behavior.

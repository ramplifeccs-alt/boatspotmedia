BoatSpotMedia v40.2 Home and Plan Sync Fix
- Fixes home runtime error by using direct /find-video link instead of missing public.find_video endpoint.
- Creator dashboard/upload/pricing now use the real plan assigned by Owner.
- Adds plan snapshot helper so storage limit, max batch limit, and prices stay synced.
- Keeps v40 metadata, latest 3 videos, and dynamic active locations.

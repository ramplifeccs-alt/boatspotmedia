BoatSpotMedia v42.5 Checkout Session Alias Fix
Base: v42.4 Order UserID + Cart Clear + Icon Fix.
Fix:
- Fixes UnboundLocalError in /cart/checkout caused by Flask session vs Stripe session variable conflict.
- Uses flask_session for logged-in buyer metadata.
- Does not touch auth, orders, cart logic, icon, upload, R2, pricing, or Google login.

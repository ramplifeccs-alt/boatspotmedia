BoatSpotMedia v41.2 Cart Success Privacy Discount Fix
Base: v41.1 Cart Snapshot Payment Fix.
Fixes:
- Buyer does not see real file name in cart before payment.
- Cart shows generic "Boat video"; file name appears only after payment/download delivery.
- Discount pending message only appears if creator discount setting is enabled.
- Payment success uses safe wrapper so cart payment does not show error if order persistence/email has an issue.
- Cart order insert uses RETURNING id instead of currval.
- Temporarily disables multi-link SendGrid cart email block to avoid bad-link email errors until full delivery workflow.
Not touched:
- upload
- R2 delete
- thumbnails generation
- pricing setup

BoatSpotMedia v39.1 Logo + R2 Cancel/Delete Fix
Base used: boatspotmedia_v39_logo_header_favicon_fix.zip
- Header logo updated to horizontal BoatSpotMedia wordmark from new image.
- Existing favicon/icon kept.
- Does not touch thumbnails.
- Does not touch working search/date/time logic.
- Adds stronger R2 cleanup:
  - deletes direct DB keys from many possible Video attributes
  - scans Video row string fields for R2-looking keys
  - deletes common batch prefixes
  - marks batch and videos as deleted
- Cancel Upload calls /r2-clean/batch/<id> when possible, otherwise /r2-clean/current-upload.
- Delete Batch routes/forms are reinforced to use R2 cleanup.

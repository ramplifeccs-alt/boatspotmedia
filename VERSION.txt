BoatSpotMedia v43.2 Always Show Download Button
Base: v43.1.
Fix:
- My Orders now always shows a visible "Download Video" button for every order item.
- Button is inside each purchased video/order item.
- If file is unavailable, download route returns a clear message.

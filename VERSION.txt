BoatSpotMedia v41.1 Cart Snapshot Payment Fix
Base: v41.0 Cart Checkout Session Fix.
Fix:
- /cart/checkout now generates a cart_id before sending buyer to Stripe.
- Saves cart snapshot in bsm_pending_cart before Stripe checkout.
- /payment/success can rebuild the purchased cart even if Flask session is empty after Stripe redirect.
- Stripe metadata now includes cart_id.
- Fixes paid cart not being associated with videos/items after payment.
Not touched:
- cart thumbnails
- upload
- R2 delete
- pricing
- SendGrid logic

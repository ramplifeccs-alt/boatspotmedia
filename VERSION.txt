BoatSpotMedia v39.8 Cancel Uses Delete Batch Flow
Base used: boatspotmedia_v39_7_cancel_manifest_r2_fix.zip
- Does not touch thumbnails.
- Does not touch search.
- Does not touch New York time/date.
- Cancel Upload now shows confirmation popup.
- If user chooses Cancel/No, upload continues.
- If user chooses OK/Yes, it calls the same R2 cleanup endpoint used by Delete Batch: /r2-clean/batch/<batch_id>.
- If batch_id is not visible in JS, it calls /cancel-upload-delete-current, which finds current batch and runs delete cleanup.

BoatSpotMedia v42.1 Buyer Register Route Fix
Base: v42.0 FULL.
Fix:
- /buyer/register is now a real registered route on buyer_bp.
- /buyer/login is now a real registered route on buyer_bp.
- Register no longer falls into Not Found after submitting name/email/password.
- Uses existing User table/auth fields.
- Does not create buyer_account table.
- Keeps Google login untouched.
- Buyer dashboard shows orders by logged-in email.

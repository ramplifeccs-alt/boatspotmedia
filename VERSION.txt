BoatSpotMedia v37 Creator Deleted Column Boot Fix
- Creates creator_profile.deleted before any Owner/Creator ORM query uses it.
- Fixes PostgreSQL UndefinedColumn: creator_profile.deleted does not exist.
- Keeps delete creator behavior and video_batch FK fix.

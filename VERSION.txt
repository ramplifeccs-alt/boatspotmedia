BoatSpotMedia v50.5A Buyer SMS Edited Ready
Base: v50.4Z rollback clean package.
Adds:
- Public buyer register saves phone_number and sms_notifications_enabled.
- Phone numbers normalize automatically:
  9545551234 -> +19545551234
  19545551234 -> +19545551234
  +19545551234 stays as-is.
- Buyer register phone field defaults to +1 in the UI.
- When edited video is uploaded and the email notification runs, system also sends SMS via Twilio if buyer has phone + SMS enabled.
Required Railway variables for SMS:
  TWILIO_ACCOUNT_SID
  TWILIO_AUTH_TOKEN
  TWILIO_PHONE_NUMBER
Does not touch R2 uploads, batches, checkout, billing, Stripe, analytics, downloads, or payouts.

BoatSpotMedia v50.5B Google OAuth Route Fix
Base: v50.5A.

Adds real buyer Google OAuth routes:
- /auth/google
- /auth/google/buyer
- /login/google
- /buyer/google
- /auth/callback/google
- /auth/google/callback

Google Cloud Authorized redirect URI:
https://boatspotmedia.com/auth/callback/google

Railway variables:
GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET

Keeps buyer phone/SMS, forgot password, checkout, uploads, R2, billing, Stripe, payouts, batches, analytics untouched.

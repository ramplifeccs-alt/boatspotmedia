BoatSpotMedia v46.1 Creator Orders Route Fix
Base: v46.0.
Real fix:
- Corrects the actual route used by /creator/orders: @creator_bp.route("/orders").
- Uses current_creator().id instead of session user_id, because video.creator_id references creator_profile.id.
- Search works by buyer email or order number.
- Fallback includes old OrderItem table if legacy purchases exist there.
- Removes mistaken /creator/creator/orders route.

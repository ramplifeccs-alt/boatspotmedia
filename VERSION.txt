BoatSpotMedia v41.5 Midpoint Thumbnail Fix
- Thumbnail is now captured from the middle of each unique video.
- Reads video duration using ffmpeg output, not ffprobe.
- Captures exact 50% duration frame first.
- If 50% is dark, tries 45%, 55%, 40%, 60%, 35%, 65%.
- Uses accurate seek with -ss after -i for camera/keyframe files.
- Keeps current R2 cleanup and upload fixes.

BoatSpotMedia v36.3 File Path + Duplicate Upload Fix
- Saves file_path and thumbnail_path to satisfy database NOT NULL constraints.
- Blocks duplicate file names per creator before upload and tells which batch already has the file.
- Keeps progress text as Uploading to BoatSpotMedia Storage.

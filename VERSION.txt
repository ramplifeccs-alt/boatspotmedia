BoatSpotMedia v43.4 Download Route Fallback Fix
Base: v43.3.
Fix:
- /buyer/download-item/<id> now accepts order_item_id OR video_id.
- Verifies the video belongs to a paid buyer order.
- Looks for the original video file across multiple possible video columns.
- Gives clear error if old order has no linked item/file.

BoatSpotMedia v41.3 Force FFmpeg and R2 Delete Fix
- Forces every creator.py subprocess call from literal ffmpeg/ffprobe to _ffmpeg_bin()/_ffprobe_bin().
- Adds imageio-ffmpeg dependency and logs actual ffmpeg path used.
- Adds explicit cancel route /upload/batch/<id>/cancel to delete R2 prefix and DB batch.
- Adds explicit full delete route /batch/<id>/delete-full to delete R2 prefix and DB batch.
- Patches existing batch delete code to call _delete_batch_files_from_r2(batch).

BoatSpotMedia v43.5 Direct Video Download Routes
Base: v43.4.
Fix:
- Adds three download routes:
  /download-video/<video_id>
  /download-item/<video_id>
  /buyer/download-item/<video_id>
- Adds routes in public.py and buyer.py so they register even if one blueprint path is not active.
- My Orders button uses /download-video/<video_id> directly.
- Verifies the logged-in buyer purchased that video, then redirects to the saved video path/key.

BoatSpotMedia v38.1 Upload UI + Thumbnail Fix
- Cancel Upload button appears during upload, not only after finishing.
- Price fields are hidden only inside upload form; Pricing menu remains visible.
- Creator pages get a top bar with BoatSpotMedia Home link, Products, creator email, and Logout.
- Browser thumbnail capture now uses middle frame, 30% zoom crop, and retries if the frame is black.

BoatSpotMedia v39.9 Recorded Date DB Fix
- Fixes database error: recorded_date cannot be null.
- Adds safe fallback recorded_date and recorded_time when metadata is missing.
- Adds best-effort R2 cleanup if DB save fails after upload.
- Keeps multipart, fallback upload, and global progress.

BoatSpotMedia v49.1K Download No public_url Column Fix
Base: v49.1I.
Fixes:
- /download-video no longer queries video.public_url because that column does not exist.
- Uses video.file_path first, then r2_video_key, then internal_filename.
- Keeps My Orders query untouched.
- Does not touch Google, Apple, login, upload, checkout, creator approval, batches, or DB.

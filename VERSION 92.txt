BoatSpotMedia v50.5Q Buyer Support Safe Fallback
Base: v50.5P.

Fix:
- /buyer/support now has a safe fallback and should not return 500.
- Each buyer support query is isolated with try/except.
- If DB/template fails, route returns a basic safe HTML page instead of Internal Server Error.
- Adds clear log markers:
  buyer support safe fallback v50.5Q
  buyer support threads safe fallback v50.5Q
  buyer support orders safe fallback v50.5Q
  buyer support render safe fallback v50.5Q

Does not touch uploads, R2, batches, checkout, billing, Stripe, payouts, Google OAuth, SMS, Creator Support, or Owner Support.

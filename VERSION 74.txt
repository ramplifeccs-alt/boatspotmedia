BoatSpotMedia v50.4X Server Edited Upload Real Route
Base: v50.4W.
Fix:
- Disables browser direct R2 PUT for edited video uploads.
- Uses real Flask server route /creator/order-item/<id>/upload-edited.
- Adds clean blueprint alias /order-item/<id>/upload-edited that calls the existing v46.3 server upload implementation.
Reason:
- Direct browser -> R2 is failing with Network error, likely CORS/browser PUT issue.
Does not touch buyer auth, checkout, billing, Stripe, payouts, batches, analytics, downloads, or R2 config.

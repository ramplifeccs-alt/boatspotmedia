BoatSpotMedia v50.4S Fix Edited Upload After Buyer Auth
Base: v50.4R.
Fix:
- Edited video upload in Creator Orders now falls back to secure server-side upload if browser direct R2 PUT fails with network/CORS error.
- Keeps direct R2 presigned upload when it works.
- Keeps buyer auth/register fixes.
Does not touch checkout, R2 service config, batches, analytics, billing, Stripe Connect payouts, downloads, or My Orders.
